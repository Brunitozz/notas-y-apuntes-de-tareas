#include <bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_studio(false);
    cin.tie(0);
    int n; cin>>n;
    for(int i=0; i<n;i++){
        int x, y, z;
        //Ingrese las coordenadas
        scanf("%d %d %d",&x, &y, &z); 
        sum=0;
        sum+=x+y+z;
    }
    if(sum==o) cout<<"YES";
    else if cout<<"NO";
    else if
    return 0;
}